#version 450
#extension GL_EXT_buffer_reference : require
layout(local_size_x = 4) in;

layout(buffer_reference) buffer Blah
{
    uint boom[];
};

layout(push_constant) uniform Registers
{
    Blah ptr;
};

void main()
{
    ptr.boom[gl_GlobalInvocationID.x] = 1;
}