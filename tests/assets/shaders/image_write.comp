#version 450
layout(local_size_x = 8, local_size_y = 8) in;

layout(set = 0, binding = 0, rgba8) writeonly uniform image2D uImage;

void main()
{
    imageStore(uImage, ivec2(gl_GlobalInvocationID.xy), vec4(0.1, 0.1, 1.0, 1.0));
}